The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.The configured pytest options create a self-contained HTML report at `reports/report.html`. Failed UI tests save screenshots under `screenshots/`. Both locations, along with `logs/`, are local generated output and are excluded from Git.
